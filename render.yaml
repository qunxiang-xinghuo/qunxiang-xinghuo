services:
  - type: web
    name: qx-xinghuo
    runtime: node
    plan: free
    buildCommand: cd frontend && npm install
    startCommand: cd frontend && npm install && npx vite --host 0.0.0.0 --port 5173
    envVars:
      - key: NODE_ENV
        value: production
