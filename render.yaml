services:
  - type: web
    name: qx-xinghuo
    runtime: node
    plan: free
    buildCommand: cd frontend && npm install
    startCommand: cd frontend && npx vite --host 0.0.0.0 --port     envVars: []
